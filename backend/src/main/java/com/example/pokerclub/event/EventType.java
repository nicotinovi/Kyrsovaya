package com.example.pokerclub.event;
//здесь просто тип мероприятия описывается
//enum это перечисление фиксированного набора значений(то есть EventType может быть только одним из
//двух признаков
public enum EventType {
    TOURNAMENT,
    TRAINING
}

